[project]
name = "openclaw-skill"
version = "0.1.0"
description = "A simple OpenClaw skill scaffold"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools","wheel"]
build-backend = "setuptools.build_meta"
